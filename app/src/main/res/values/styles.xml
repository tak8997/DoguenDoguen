<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Splash.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorBrown</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyAppBarLayout" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>

    <!-- HomeActivity Tablayout -->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">#FF3D00</item>
        <item name="tabIndicatorColor">#FFFFFF</item>
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">#7F7F7F</item>
    </style>

    <!-- MessageDetailActivity Theme -->
    <style name="AppTheme.Message" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:actionOverflowButtonStyle">@style/OverFlow</item>
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorBrown</item>
    </style>

    <!-- Styles -->
    <style name="OverFlow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/overflow</item>
    </style>

    <style name="MyDialogTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/dialog_background</item>
    </style>

    <style name="LoginDialogTheme">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@drawable/dialog_background</item>
    </style>

    <!-- Spinner Style -->
    <style name="Spinner_style" >
        <item name="android:background">@drawable/spinner_bg</item>
        <!--<item name="android:layout_marginLeft">22dp</item>-->
        <item name="android:layout_marginRight">8dp</item>
        <!--<item name="android:layout_marginBottom">10dp</item>-->
        <!--<item name="android:paddingLeft">8dp</item>-->
        <!--<item name="android:paddingTop">5dp</item>-->
        <!--<item name="android:paddingBottom">5dp</item>-->
    </style>

</resources>
